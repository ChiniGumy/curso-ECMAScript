let numbers = [1, 3, 4, 5, 9];

console.log(numbers.includes(2));		// Devuelve bool


const list = ['Dome', 'Macias', 'David', 'Miguel'];

console.log(list.includes('Dome'));					// El string si o si tiene que estar igual, con mayuscula y minuscula